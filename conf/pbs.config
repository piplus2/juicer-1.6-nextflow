// Example of basic PBS configuration for Juicer Nextflow pipeline
// Adjust resource parameters as needed

includeConfig "${baseDir}/conf/process_resources.config"

process {
    executor = 'pbs'
    queue    = params.hpc_queue ?: 'workq'
    scratch  = true
}

executor {
    name = 'pbs'
}
