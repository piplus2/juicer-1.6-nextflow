singularity {
    enabled    = true
    autoMounts = true
    cacheDir   = params.singularity_cache_dir ?: null
}

docker {
    enabled = false
}

conda {
    enabled = false
}

process {
    withLabel: gpu {
        containerOptions = '--nv'
    }
}